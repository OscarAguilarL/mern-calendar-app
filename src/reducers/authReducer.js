const initState = {
    checking: true,
    // uid: null,
    // name: null,
};

export const authReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
